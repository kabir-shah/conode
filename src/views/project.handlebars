<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<meta name="description" content="">

		<title>Conode</title>

		<link href="https://fonts.googleapis.com/css?family=Poppins|Inconsolata" rel="stylesheet">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/9.15.6/styles/nord.min.css"/>
		<link rel="stylesheet" type="text/css" href="/css/styles.css"/>
		<link rel="stylesheet" type="text/css" href="/css/project.css"/>
	</head>
	<body>
		<div class="container">
			<div class="nav">
				<div class="nav-left">
					<h3 id="logo"><a href="/">Conode</a></h3>
				</div>
				<div class="nav-right">
					<p id="logo"><a href="/projects/create">Create a project</a></p>
				</div>
			</div>

			<h1>{{project.title}}</h1>
			<h3>By {{project.author}}</h3>
			<h5>Created on {{project.date}}</h5>
			<h6>{{project.likes}} Likes</h6>
			<img src="{{project.image}}" id="project-image"/>
			<h5 class=secondary-text>{{project.description}}</h5>
			<p>{{{project.content}}}</p>
			<p>
				{{#each project.topics}}
					<span class="topic box">{{this}}</span>
				{{/each}}
			</p>

			<form action="/projects/{{project._id}}/teams/create" method="POST">
				<input type="text" name="name" placeholder="Name"/>
				<input type="email" name="email" placeholder="Email"/>
				<input type="number" id="max" name="max" placeholder="Maximum number of members"/>
				<button>Create a team</button>
			</form>

			<form action="/projects/{{project._id}}/teams/join" method="POST">
				<input type="text" name="name" placeholder="Name"/>
				<input type="email" name="email" placeholder="Email"/>
				<input type="text" id="code" name="code" placeholder="Team code"/>
				<button>Join a team</button>
			</form>
		</div>
		<script src="//cdnjs.cloudflare.com/ajax/libs/highlight.js/9.15.6/highlight.min.js"></script>
		<script src="/js/highlight.js"></script>
	</body>
</html>
